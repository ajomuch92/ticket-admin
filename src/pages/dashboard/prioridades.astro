---
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import { Plus, SquarePen } from "@lucide/astro";

// Datos simulados
const prioridades = [
    { id: 1, nombre: "Baja", orden: 1, color: "green" },
    { id: 2, nombre: "Media", orden: 2, color: "blue" },
    { id: 3, nombre: "Alta", orden: 3, color: "orange" },
    { id: 4, nombre: "Crítica", orden: 4, color: "red" },
];

const badgeClass = (c: string) =>
    ({
        blue: "bg-primary",
        orange: "bg-warning text-dark",
        green: "bg-success",
        red: "bg-danger",
        gray: "bg-secondary",
    })[c] ?? "bg-secondary";
---

<DashboardLayout pageTitle="Gestión de Prioridades">
    <div class="card">
        <div class="card-header bg-white d-flex align-items-center justify-content-end">
            <button class="btn btn-primary d-flex align-items-center gap-2">
                <Plus width="16" height="16" stroke-width="2" />
                Nueva Prioridad
            </button>
        </div>

        <div class="table-responsive">
            <table class="table table-hover align-middle m-0">
                <thead class="table-light">
                    <tr>
                        <th class="text-uppercase small text-secondary">ID</th>
                        <th class="text-uppercase small text-secondary">Prioridad</th>
                        <th class="text-uppercase small text-secondary text-center">
                            Orden
                        </th>
                        <th class="text-uppercase small text-secondary text-end">
                            Acciones
                        </th>
                    </tr>
                </thead>
                <tbody>
                    {
                        prioridades.map((prioridad) => (
                            <tr>
                                <td class="text-secondary fw-medium">
                                    #{prioridad.id}
                                </td>
                                <td>
                                    <span class={`badge ${badgeClass(prioridad.color)}`}>
                                        {prioridad.nombre}
                                    </span>
                                </td>
                                <td class="fw-semibold text-secondary text-center">
                                    {prioridad.orden}
                                </td>
                                <td class="text-end">
                                    <button class="btn btn-sm btn-link text-secondary p-1">
                                        <SquarePen width="18" height="18" stroke-width="2" />
                                    </button>
                                </td>
                            </tr>
                        ))
                    }
                </tbody>
            </table>
        </div>
    </div>
</DashboardLayout>
